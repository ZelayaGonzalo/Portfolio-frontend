export class Theme{
    background:string
    text:string
    color:string
    constructor(background:string,text:string,color:string){
        this.background = background
        this.text = text
        this.color =color
    }
}